package fiuba.algo3.modelo.excepciones;

public class CeldaNoOcupadaException extends RuntimeException {
    //Excepción a lanzar cuando se trata de acceder a una celda que no está ocupada.
    //Por ejemplo, tratar de liberar una celda del tablero que no está ocupada.
}
